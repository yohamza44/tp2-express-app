html
  head
    title Login
    //- link(rel="stylesheet" href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css")
    script(src="https://cdn.tailwindcss.com")

  body
    div.flex.flex-col.items-center.justify-center.min-h-screen
      h1.text-2xl.font-semibold.text-center.mb-4 Login
      form(action="/login" method="POST" class="w-80 max-w-md p-4 border rounded-lg shadow-md")
        label(for="username" class="block font-semibold") Username:
        input(type="text" id="username" name="username" class="w-full px-3 py-2 border rounded-md focus:outline-none focus:ring focus:border-blue-500")
        label(for="password" class="block font-semibold") Password:
        input(type="password" id="password" name="password" class="w-full px-3 py-2 border rounded-md focus:outline-none focus:ring focus:border-blue-500")
        button(type="submit" class="w-full mt-4 px-3 py-2 font-semibold text-white bg-blue-500 rounded-md hover:bg-blue-600 focus:outline-none focus:ring focus:bg-blue-400") Login
      p.text-center.mt-2
       | Don't have an account? 
       a(href="/register", class="text-blue-500 hover:underline") Register here
